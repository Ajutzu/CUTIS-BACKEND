<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cutis API - Running</title>
  <link rel="icon" href="../assets/icox.png">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">

  <style>
    body {
      font-family: 'Inter', system-ui, sans-serif;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .pulse-dot {
      width: 12px;
      height: 12px;
      background-color: #22c55e;
      border-radius: 50%;
      animation: pulse 2s infinite;
    }
    @keyframes pulse {
      0%, 100% { transform: scale(1); opacity: 1; }
      50% { transform: scale(0.8); opacity: 0.5; }
    }
    .float {
      animation: float 6s ease-in-out infinite;
    }
    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-20px); }
    }
  </style>
</head>
<body class="bg-white">
  <div class="container py-5">
    <div class="row align-items-center justify-content-center g-5 text-center text-lg-start">
      <!-- Left Content -->
      <div class="col-lg-6 d-flex flex-column align-items-center align-items-lg-start">
        <div class="d-flex align-items-center mb-4 justify-content-center justify-content-lg-start">
          <img src="/assets/icox.png" alt="Cutis Logo" class="rounded-circle mb-3 me-2" width="70" height="70">
          <div>
            <h1 class="fw-bold text-dark mb-1">Cutis API</h1>
            <p class="text-muted small mb-0">Powered by Node.js & Express</p>
          </div>
        </div>

        <!-- Status -->
        <div class="d-inline-flex align-items-center border border-success-subtle bg-success-subtle rounded-pill px-3 py-2 mb-4">
          <div class="pulse-dot me-2"></div>
          <span class="text-success fw-semibold small">API is operational</span>
        </div>

        <!-- Description -->
        <div class="mb-4 text-center text-lg-start">
          <h4 class="fw-semibold text-dark">AI-Powered Skin Condition Analysis</h4>
          <p class="text-muted">
            Cutis is an intelligent system that provides real-time skin analysis, 
            web-based insights, and healthcare connectivity — empowering patients with AI-driven accuracy and care.
          </p>
        </div>

      </div>

      <!-- Right Image -->
      <div class="col-lg-6 d-flex justify-content-center">
        <div class="position-relative">
          <img src="/assets/background.png" alt="Healthcare Dashboard" class="img-fluid position-relative float" style="max-width: 550px;">
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
